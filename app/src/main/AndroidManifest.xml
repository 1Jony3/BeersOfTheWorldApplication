<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.cft.shift2021summer">

    <application
        android:name=".app.BreakingBadApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shift2021Summer">
        <activity android:name="ru.cft.shift2021summer.feature.asteroid.details.presentation.AsteroidDetailsActivity"></activity>
        <activity android:name="ru.cft.shift2021summer.feature.asteroid.list.AsteroidListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>