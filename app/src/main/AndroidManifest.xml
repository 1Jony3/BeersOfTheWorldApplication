<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.cft.shift2021summer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.BeersOfTheWorldApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shift2021Summer">
        <activity android:name=".feature.breweries.search.SearchActivity"></activity>
        <activity android:name=".feature.breweries.list.BeersListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.breweries.details.presentation.BeersDetailsActivity" />
    </application>

</manifest>